## plot genometric correlations for a specific region and metric
## will probably wrap plotAsGviz in the very near future...
##
## FIXME: handle WIG tracks and other things with scores/depths
##
plotGenometriCorr <- function(object, GR, colors=NULL, ...) {
  require(GenometriCorr)
  stop('Plotting of genometric correlations is not finished yet')
}
